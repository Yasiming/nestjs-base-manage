export enum UserConstants {
  ADMIN = "11",
  COMMON = "00",
}

export enum RoleKeyConstants {
  ADMIN = "admin",
  DEFAULT = "common",
}

export enum MenuTypeConstants {
  M = "M",
  C = "C",
  F = "F",
}
